# Machine-local Zsh overrides live in $HOME/.zshrc.local.

# Example aliases
# alias gs='git status -sb'

# Example PATH extension
# export PATH="$HOME/bin:$PATH"

# Example: source a machine-private env file
# [ -f "$HOME/.config/shell/private-env.zsh" ] && . "$HOME/.config/shell/private-env.zsh"
